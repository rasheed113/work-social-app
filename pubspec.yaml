name: work_social_app
description: Native Flutter Android client for Work Social.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  supabase_flutter: ^2.17.2
  go_router: ^16.2.0
  image_picker: ^1.2.0
  file_picker: ^10.3.3
  geolocator: ^14.0.2
  mime: ^2.0.0
  uuid: ^4.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
